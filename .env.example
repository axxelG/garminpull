# Copy to .env and fill in. Only needed for the first login (or after tokens expire);
# afterwards garminpull reuses the tokens stored under ~/.garminconnect.
GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=your-password

# Optional: override where OAuth tokens are cached.
# GARMINTOKENS=~/.garminconnect
